# Inference Data & Model Result Analysis

## Background
최근 수집된 `docs/inference_reports/` 하위의 인퍼런스 에피소드 및 세션 데이터를 원격 저장소(`origin/vla-driving`)에 강제(force add) 병합 및 푸시 완료하였습니다. 동시에, 새롭게 수집된 데이터(총 15개 세션)가 어떤 형태로 구성되어 있고, 동일한 명령어에 대해 모델의 행동(Action)이 어떻게 차이를 보이는지 통계적 관점에서 비교 분석하였습니다.

## Analysis

### 1. Episode 및 Session 데이터 포맷
수집된 각각의 에피소드는 하나의 **JSON 메타데이터 파일**과 **이미지 디렉토리**의 하이브리드 형태로 구성되어 있습니다. `session_{timestamp}.json`의 구조는 아래와 같습니다:

- **Metadata Information**:
  - `session_id`: 고유 세션 ID (예: "20260305_100638")
  - `timestamp`: 세션 시작 시각
  - `model_name`: 사용 모델 (예: `mobile_vla_v3_exp07_lora`)
  - `instruction`: 사용자 입력 지시어 (예: "Navigate to the brown pot on the left")
  - `status`, `end_time`: 세션 종료 상태 및 시점
- **History (궤적 정보)**: 각 스텝(프레임) 단위의 세부 로그가 배열 형태로 기록됨
  - `step`: 프레임 (스텝) 번호
  - `timestamp`: 인퍼런스 및 행동 결정 시각
  - `action`: 모델이 예측한 최종 제어값 `[Linear Velocity, Angular Velocity]`
  - `latency_ms`: 인퍼런스 지연 시간 (단위: ms)
  - `chunk_preview`: 모델이 예측한 미래 궤적의 원시 복수 벡터 (Chunking Prediction)
  - `image_file`: 해당 프레임에서 수집된 카메라 이미지 경로 (예: `session_20260305_100638_images/frame_001.jpg`)
- **Summary**: `total_steps`, `avg_latency`, `last_action` 등 에피소드 요약

### 2. 동일 명령어 환경에서의 Model Result 차이 분석
수집된 최근 15개 세션 모두 모델 지시어는 `"Navigate to the brown pot on the left"` 이며, 모델명은 `mobile_vla_v3_exp07_lora`로 동일하게 구동되었습니다. 모델 Action의 주요 통계 결과는 다음과 같습니다:

| Session ID      | Steps | Avg Latency | Linear Action | Angular Distribution (0.0 / -1.15 / 1.15 빈도) | Main Tendency               |
| --------------- | ----- | ----------- | ------------- | ---------------------------------------------- | --------------------------- |
| 20260305_100638 | 34    | 595.3ms     | ~1.08         | 정지: 24, 좌회전(-): 2, 우회전(+): 8           | 직진 및 우편향 조향         |
| 20260305_100719 | 34    | 551.5ms     | ~1.08         | 정지: 16, 좌회전(-): 12, 우회전(+): 6          | 약간의 좌편향 조향          |
| 20260305_101634 | 34    | 547.5ms     | ~1.08         | 정지: 14, 좌회전(-): 16, 우회전(+): 4          | **강한 좌회전** 성향        |
| 20260305_102030 | 34    | 546.4ms     | ~1.08         | 정지: 16, 좌회전(-): 6, 우회전(+): 12          | **강한 우회전** 성향        |
| 20260305_103454 | 34    | 559.9ms     | ~1.08         | 정지: 12, 좌회전(-): 16, 우회전(+): 6          | 강한 좌회전 성향            |
| 20260305_104605 | 34    | 549.0ms     | ~1.08         | 정지: 18, 좌회전(-): 6, 우회전(+): 10          | 우회전 경향, 높은 직진 비율 |
| 20260305_104859 | 34    | 549.7ms     | ~1.08         | 정지: 10, 좌회전(-): 14, 우회전(+): 10         | 좌/우 조향 잦음 (지그재그)  |

*(참고: 총 15개 세션 중 특징적인 분포를 가진 세션을 발췌)*

## Findings

1. **안정적인 직진 가속 제어**:
   - `Linear Velocity`의 평균은 모든 세션에서 **1.08**로 거의 동일합니다. 최대치 1.15의 가속 명령이 안정적이고 일관적으로 호출되고 있습니다. 인퍼런스 Latency 역시 평균 550ms 내외로 일정합니다.
   
2. **Angular Velocity (조향)의 높은 분산성 (Variance)**:
   - 동일한 지시어("... on the left")에 대해 초기/연속된 프레임 이미지의 미세한 환경적 노이즈나 위치에 따라 각기 다른 조향비(좌회전: -1.15, 우회전: 1.15)를 섞어서 예측하고 있습니다.
   - 예컨대, `101634`, `103454` 세션의 경우 좌회전 액션 횟수(16회)가 지배적이지만, `102030` 세션에서는 우회전 액션 횟수(12회)가 월등히 높아 정반대의 거동 성향을 나타냅니다.

3. **대상 소실 시 회전 중단 현상 (Reactive Behavior)**:
   - 로그 분석(`session_20260305_104859` 등) 결과, 브라운 팟이 시야(FOV)에서 사라지는 순간 `Angular Velocity`값이 수 차례 연속으로 **0.0**으로 수렴하는 경향이 포착되었습니다.
   - 이는 모델이 대상의 위치를 기억(Memory/Object Permanence)하고 추적하는 것이 아니라, 매 프레임 이미지 내에 대상이 존재할 때만 조건부로 조향을 수행하는 **Reactive Policy**에 치중되어 있음을 시사합니다.

## Proposed Data Collection Strategy

현재 모델은 대상이 시야에서 사라지면 '탐색(Search)'하거나 '기존 방향으로 지속'하는 능력이 부족합니다. 성능 개선을 위해 다음과 같은 에피소드 추가 수집이 필요합니다:

- **Recovery/Search Episodes**: 대상이 시야 밖으로 나갔을 때, 다시 시야 안으로 들어올 때까지 지속적으로 회전하거나 주변을 살피는 '회복' 거동이 포함된 데이터.
- **Occlusion Robustness**: 대상이 가구 등에 의해 부분적으로 가려진 상태에서도 조향을 유지하는 케이스 확대.
- **Varying Initial Angles**: 시작 시점부터 대상이 시야 구석에 있거나 아슬아슬하게 걸쳐 있는 상황을 많이 노출시켜 FOV 경계면에서의 정책을 강화.

## Conclusion

수집 데이터를 검토한 결과, 모델은 시작 지점에서 직진(Linear)은 안정적으로 유지하나 조향(Angular) 정책이 주변 환경 피처(관측 이미지) 및 대상의 가시성(Visibility)에 매우 민감하게 반응하고 있습니다. 특히 대상이 사라지면 회전을 멈추는 한계가 명확하므로, **객체 지속성(Object Permanence)을 학습할 수 있는 탐색 및 회복 시나리오 위주의 추가 데이터 수집**이 필수적입니다.
