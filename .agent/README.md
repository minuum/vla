# Antigravity Project Configuration

This directory (`.agent`) is used to configure Antigravity for this specific project.

## Workflows
You can define custom workflows in the `workflows/` subdirectory.
Workflows are Markdown files that describe a series of steps for Antigravity to follow.

Example: `.agent/workflows/deploy.md`
